interface:
  display_name: "Xia Research Scout"
  short_description: "Map the repo before you build"
  default_prompt: "Use $xia to map this repo, find reusable local code, check upstream patterns and current docs, then recommend the lightest path before we implement anything."
